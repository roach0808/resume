# Streamlit Secrets Configuration Example
# 
# For cloud deployment (Streamlit Cloud, etc.):
# 1. Copy this file to .streamlit/secrets.toml (without .example)
# 2. Replace the placeholder with your actual OpenAI API key
# 3. DO NOT commit secrets.toml to version control (it should be in .gitignore)
#
# For local development:
# You can still use environment variables or .env file as a fallback
# The code will automatically try Streamlit secrets first, then fall back to environment variables

OPENAI_API_KEY = "your_openai_api_key_here"

# Optional: If you use Hyperbolic LLM API
# HYPERBOLIC_API_KEY = "your_hyperbolic_api_key_here"

